import 'package:flutter/material.dart';

class AppColors {
  // Primary palette
  static const primary = Color(0xFF09637E);
  static const secondary = Color(0xFF088395);
  static const accent = Color(0xFF7AB2B2);
  static const background = Color(0xFFEBF4F6);

  // Text colors
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);

  // Status colors (important for expense app)
  static const income = Color(0xFF16A34A);
  static const expense = Color(0xFFDC2626);
}
